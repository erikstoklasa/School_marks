<shortcuts xmlns:android="http://schemas.android.com/apk/res/android">
    <shortcut
        android:shortcutId="dashboard"
        android:enabled="true"
        android:icon="@drawable/ic_dashboard_black_48dp"
        android:shortcutShortLabel="@string/dashboard"
        android:shortcutLongLabel="@string/dashboard"
        android:shortcutDisabledMessage="@string/dashboard_disabled_message1">
        <intent
            android:action="android.intent.action.VIEW"
            android:targetPackage="cz.erikstoklasa.schoolmarks"
            android:targetClass="cz.erikstoklasa.schoolmarks.MainActivity" />
        <!-- If your shortcut is associated with multiple intents, include them
             here. The last intent in the list determines what the user sees when
             they launch this shortcut. -->
        <categories android:name="android.shortcut.dashboard" />
    </shortcut>
    <!-- Specify more shortcuts here. -->
</shortcuts>